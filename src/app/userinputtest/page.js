import { dbConnect } from "../utils/dbConnection";
import UserFormTest from "../components/FormTest";

export default async function UserInputTest() {
  //   const db = dbConnect();

  //   const inputtedInfo = await db.query;

  return (
    <div>
      <UserFormTest></UserFormTest>
    </div>
  );
}
